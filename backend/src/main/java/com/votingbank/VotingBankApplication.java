package com.votingbank;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Voting Bank API application.
 * Secure Digital Voting System backend powered by Spring Boot.
 */
@SpringBootApplication
public class VotingBankApplication {

    public static void main(String[] args) {
        SpringApplication.run(VotingBankApplication.class, args);
    }
}
